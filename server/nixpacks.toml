[phases.setup]
nixPkgs = ['python3', 'nodejs', 'curl']

[start]
cmd = "npm run build && curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp && chmod +x yt-dlp && node dist/index.js" 